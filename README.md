# ls.py
`ls.py` is clone of `ls` utility in linux, which list files.
It uses `os` module to get file list.

**NOTE:** This program only works above Python 3.10.

## Usage
```shell
git clone https://github.com/SharathKumarK06/ls.py
cd ls.py
./ls.py
```

